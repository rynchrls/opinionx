import { createTheme } from "@mui/material";

const theme = createTheme({
  typography: {
    fontFamily: "'Poppins', sans-serif", // Using a clean and modern font for the main text
  },
});

export default theme;